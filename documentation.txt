SimObject : abstract class
 + id, width , height.

Animated object : child of SimObject
 + speed, curspeed, Orietation
 + all getter. and sette

inAnimatedObject < SimObject
 + isInteractable, isCollidable
 
vehicles < AnimatedObject:
 + Road : the road that the vehicles it belong to
 + Original Road : after perform turn the road field is changed to it's new road,  To restore it back to original road we iuse this feidl.
 + turn direction : a value to know which direction it's going to turn to(left right or forward.)

Car < vehicles:
 + 